# AI开发流框架开发规则

## 真相源与边界

- `ai-flow.config.json` 路由框架配置，`ai-dev/baseline.json` 路由当前基线；产品事实以基线登记的 canonical 真相源为准。
- `docs/framework-spec.md` 是本框架当前内容真相源。README、示例、访谈和外部项目只能作为说明或输入，不得覆盖它。
- 通用核心不得包含具体产品、厂商、云环境、硬件数量、密钥或部署值；项目差异通过配置、契约和验证器注入。
- `starter/**` 是可复制骨架，不是任何新项目的产品真相源。初始化后，项目独立拥有自己的规则、规格和证据。

## 开发闭环

1. 修改核心、契约或 starter 前，先读取配置、基线、现行规格和相关决策。
2. 实现任务必须声明目标、需求、允许路径、风险、验证器和所需证据等级。
3. 普通 implementation 与缺陷修复默认通过 `ai-flow start --mode auto` 进入；用户明确要求快速或完整流程时分别传入 `quick` 或 `full`。模式只表达偏好，确定性资格检查拥有最终决定权，`quick` 不合格时必须回落 `full`，不得绕过 TaskPacket、控制器或证据门。
4. 默认采用满足已确认目标、既有架构和必要质量约束的最小充分实现，满足 TaskPacket 验收即停；新增依赖、架构层、配置项或通用抽象必须有当前任务或既有架构的具体依据，不以代码行数、文件数或抽象数量作为目标。
5. 首次写入前和独立审查前调用可用的 `ai-flow-experience` Skill，命中案例只作 Historical reminders，不改变任务范围、授权、验收或裁判。
6. 实现者只写允许路径；不得修改规格、验收、审查规则或证据门来迁就实现。
7. 先运行确定性验证，再由不同上下文进行只读审查；返修只在原任务范围内进行。
8. 重复问题、振荡、范围扩大、真相源冲突、未决决策、轮次耗尽或未经授权的副作用必须停止自动循环。
9. 放行时绑定规格摘要、代码修订、验证器版本和输入摘要，并明确剩余风险与最高证据等级。

## 实现约束

- 开发基线为 Node.js 20+、ESM 和零运行时依赖；引入依赖必须有不可由平台能力满足的明确理由。
- `src/core/**` 保持确定性和无副作用，不调用 AI、网络、Git 或外部服务。
- CLI 不覆盖非空目录或冲突文件；`adopt` 与升级默认只读，写入必须显式请求且只能创建缺失文件。
- Schema 默认使用封闭对象；框架只维护固定 `frameworkVersion: 1.0.0` 的单一滚动最新版，兼容性内容变化通过 `distributionDigest`、兼容性检查和当前变更摘要识别，不递增 patch/minor 版本；只有产物契约不兼容时才更新对应 `schemaVersion` 和相应测试。
- 不以 AI 自述、文件存在或命令字符串作为通过证据；确定性裁判负责最终 PASS/FAIL。

## 检查与权限

- 最小总检查：`npm run check`。
- 只运行覆盖变更风险的最小验证；发布前才运行完整兼容性与黄金任务集。
- 未经明确授权，不推送、发布、部署、调用真实硬件或执行外部副作用。
- `AGENTS.md`、`ai-flow.config.json`、`ai-dev/**`、`schemas/**`、`src/core/**`、`starter/**` 和现行框架规格属于控制面。
